services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: thermocore-web:local
    ports:
      - "8080:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ASPNETCORE_URLS: http://+:8080
