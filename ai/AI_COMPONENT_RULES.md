# AI Component Rules

Each component shall:
- expose typed inputs/outputs;
- own only its internal state;
- not modify other components;
- provide diagnostics;
- expose balances when applicable;
- include unit tests.
