# Research Context

Evidence levels:

```text
A prototype measurement
B manufacturer performance data
C peer-reviewed experiment
D standard/reference correlation
E engineering assumption
```

Required workflow:

```text
specification
→ reference tests
→ bench test
→ calibration
→ holdout validation
→ integrated validation
→ outdoor validation
```

Do not promote an empirical model to validated status without recorded evidence.
